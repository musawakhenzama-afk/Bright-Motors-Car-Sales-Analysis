Total Revenue by Car Make and Model

SELECT
    MAKE,
    MODEL,
    AVG(SELLINGPRICE) AS AVG_PRICE,
    AVG(odometer) AS AVG_MILEAGE,
    COUNT(*) AS SALES_COUNT
FROM
    "BRIGHTLEARNDEALERSHIPDATA"."PUBLIC"."CARSALES"
GROUP BY
    MAKE,
    MODEL,
    YEAR
ORDER BY
    YEAR DESC, AVG_PRICE DESC;